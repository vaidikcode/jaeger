name: Dummy Workflow

on:

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Print Hello
        run: echo "Hello, world!"
